Example Shape 2:

        #####
         ###
          # 


code:- 
#include <iostream>
using namespace std;

int main() {
    int rows[] = {5, 3, 1}; // Number of '#' in each row

    for (int i = 0; i < 3; i++) {  
        for (int j = 0; j < rows[i]; j++) {  
            cout << "#";
        }
        cout << endl;
    }
    
    return 0;
}
